{
  "name": "dexscreener-mcp",
  "version": "1.0.0",
  "description": "MCP server for DexScreener — real-time DEX token data, BSC alpha scanner",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js --http",
    "dev": "tsx src/index.ts --http"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  },
  "engines": { "node": ">=18" }
}
